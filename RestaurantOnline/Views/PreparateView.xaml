<UserControl x:Class="RestaurantOnline.Views.PreparateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantOnline.Views"
             xmlns:viewmodels="clr-namespace:RestaurantOnline.ViewModels"
             xmlns:converters="clr-namespace:RestaurantOnline.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converters:ImagePathConverter x:Key="ImagePathConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Titlu -->
        <TextBlock Grid.Row="0" Text="Preparate" FontSize="24" FontWeight="Bold" Margin="20,10"/>
        
        <!-- Filtre și căutare -->
        <Grid Grid.Row="1" Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Categorie:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Width="150" ItemsSource="{Binding Categorii}" 
                          DisplayMemberPath="Nume" 
                          SelectedItem="{Binding CategorieSelectata}"
                          Margin="0,0,20,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,10,0" VerticalContentAlignment="Center"
                         Height="25" />
                <Button Content="Caută" Command="{Binding SearchCommand}" Width="80" Height="25"/>
            </StackPanel>
        </Grid>
        
        <!-- Lista de preparate -->
        <ListView Grid.Row="2" ItemsSource="{Binding Preparate}" Margin="20,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Imagine preparat cu cale relativă -->
                        <Border Grid.Column="0" Background="LightGray" Width="60" Height="60" CornerRadius="5">
                            <Grid>
                                <Image Name="PreparatImage" 
                                       Source="{Binding Fotografii[0].Url, Converter={StaticResource ImagePathConverter}}" 
                                       Stretch="UniformToFill" 
                                       Width="60" Height="60" 
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       ToolTip="{Binding Fotografii[0].Url}"
                                       ImageFailed="Image_ImageFailed">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Style.Triggers>
                                                <EventTrigger RoutedEvent="Image.ImageFailed">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Source">
                                                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Null}"/>
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock Text="NaN" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" 
                                           VerticalAlignment="Center" Foreground="Red" Visibility="{Binding ElementName=PreparatImage, Path=Source, Converter={StaticResource NullToVisibilityConverter}}"/>
                            </Grid>
                        </Border>
                        
                        <!-- Informații preparat -->
                        <StackPanel Grid.Column="1" Margin="10,0">
                            <TextBlock Text="{Binding Denumire}" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="{Binding Categorie.Nume}" FontStyle="Italic" Margin="0,5,0,0"/>
                            <TextBlock Margin="0,5,0,0">
                                <Run Text="Cantitate: "/>
                                <Run Text="{Binding CantitatePortieGrame}"/>
                                <Run Text=" g"/>
                            </TextBlock>
                            <TextBlock Text="Descriere:" FontWeight="SemiBold" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding Descriere}" TextWrapping="Wrap" Margin="5,0,0,5"/>
                            
                            <!-- Afișare îmbunătățită alergeni -->
                            <Border Background="#FFF9E9" BorderBrush="#FFE9C5" BorderThickness="1" 
                                    CornerRadius="3" Padding="5" Margin="0,5,0,0" 
                                    Visibility="{Binding PreparatAlergeni.Count, Converter={StaticResource CountToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="Alergeni: " FontWeight="SemiBold" Foreground="#C06D00"/>
                                    <ItemsControl ItemsSource="{Binding PreparatAlergeni}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#FFEED6" CornerRadius="3" Margin="0,2,5,2" Padding="3,1">
                                                    <TextBlock Text="{Binding Alergen.Nume}" FontSize="11"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        
                        <!-- Preț și butoane -->
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding Pret, StringFormat={}{0:N2} RON}" FontWeight="Bold" FontSize="16" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl> 