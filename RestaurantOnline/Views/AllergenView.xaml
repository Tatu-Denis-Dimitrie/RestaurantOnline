<UserControl x:Class="RestaurantOnline.Views.AllergenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantOnline.Views"
             xmlns:converters="clr-namespace:RestaurantOnline.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Titlu -->
        <TextBlock Grid.Row="0" Text="Gestionare Alergeni" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <!-- Eroare -->
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Red" 
                   Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="0,0,0,10" TextWrapping="Wrap"/>
        
        <!-- Lista alergeni și formular -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Lista alergeni -->
            <Border Grid.Column="0" BorderBrush="#ccc" BorderThickness="1" Margin="0,0,5,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Alergeni existenți" FontWeight="SemiBold" Margin="10,10,10,5"/>
                    <ListView DockPanel.Dock="Bottom" ItemsSource="{Binding Allergens}" 
                              SelectedItem="{Binding SelectedAllergen}" Margin="10">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DockPanel>
            </Border>
            
            <!-- Formular adăugare/editare -->
            <Border Grid.Column="1" BorderBrush="#ccc" BorderThickness="1" Margin="5,0,0,0">
                <StackPanel Margin="10">
                    <TextBlock Text="Adaugă/Editează Alergen" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Nume Alergen:" Margin="0,5"/>
                    <TextBox Text="{Binding NewAllergenName, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" Margin="0,0,0,10"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Salvează" Command="{Binding SaveCommand}" 
                                Width="100" Height="30" Margin="0,0,10,0" 
                                Background="#2ecc71" Foreground="White"/>
                        
                        <Button Content="Șterge" Command="{Binding DeleteCommand}" 
                                Width="100" Height="30" 
                                Background="#e74c3c" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl> 