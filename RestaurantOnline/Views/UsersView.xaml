<UserControl x:Class="RestaurantOnline.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RestaurantOnline.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Titlu -->
        <TextBlock Grid.Row="0" Text="Utilizatori" FontSize="24" FontWeight="Bold" Margin="20,10"/>
        
        <!-- Lista de utilizatori -->
        <ListView Grid.Row="1" ItemsSource="{Binding Utilizatori}" Margin="20,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock>
                                <Run Text="{Binding Nume}" FontWeight="Bold"/>
                                <Run Text=" "/>
                                <Run Text="{Binding Prenume}" FontWeight="Bold"/>
                            </TextBlock>
                            <TextBlock Text="{Binding Email}" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding Telefon}" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding AdresaLivrare}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Rol}" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl> 